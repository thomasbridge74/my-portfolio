version: 0.2

phases:
  build:
    commands:
      - npm install
      - npm run webpack

artifacts:
  files:
    - favicon.ico
    - images/*
    - styles/*
    - example.txt
    - ThomasBridge.CV.pdf
    - ./*.html
    - subdir/*
    - dist/bundle.js

